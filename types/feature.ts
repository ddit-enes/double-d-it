import { JSX } from "react";

export type FeatureTitleSectionType = {
  title: string;
  paragraph: string;
};

export type FeatureType = {
  icon: JSX.Element;
  title: string;
  paragraph: string;
};
